---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionHeading from '../components/SectionHeading.astro';
import { site } from '../data/site';
import { arc, education } from '../data/skills';
---

<BaseLayout
  title="About"
  description="How I went from shipping SwiftUI apps to owning release engineering for a 70+ microservice enterprise platform."
>
  <section class="shell pt-16 pb-12 sm:pt-24">
    <SectionHeading eyebrow="About" title="The longer version" />

    <div class="mt-10 grid gap-12 lg:grid-cols-[minmax(0,1fr)_18rem]">
      <div class="prose-body max-w-2xl">
        <p>
          I'm a platform engineer in Bengaluru. I work on what sits between a
          merged change and a running production service — pipelines,
          environments, permissions, telemetry and reverts.
        </p>

        <h2>I started on the other side</h2>
        <p>
          My degree specialised in iOS development, and my first job was mobile:
          a native SwiftUI iPad application that I built on my own for a client
          engagement at TCS. It shipped in about two months. Once it was stable
          and the enhancement queue emptied out, I picked up the AWS side of the
          engagement because there was nothing else to do.
        </p>
        <p>
          I liked the infrastructure work more than the app work, so I asked to
          move.
        </p>

        <h2>Platform Solutions</h2>
        <p>
          I joined the Platform Solutions unit expecting to build a mobile client
          for <strong>TCS Enterprise Manager</strong>, an enterprise ITSM/ITOM
          product. Budget constraints moved me into a DevOps role instead, around
          the time the unit decided to modernise how it delivered software.
        </p>
        <p>
          The starting point: 70+ microservices in SVN, a fixed weekly deployment
          window, no review gate, no automated evaluation of a merge, and a revert
          that took about an hour of manual work. Two of us were asked to change
          that.
        </p>
        <p>
          Most of the first stretch went on writing down the delivery model rather
          than building pipelines — what branches mean, what a merge has to pass,
          how environments get promoted, who can deploy where. The pipelines came
          after, as an implementation of that document.
        </p>
        <p>
          The revert pipeline turned out to matter more than the deploy pipeline.
          Once a revert took under thirty seconds instead of an hour, the team
          stopped saving changes for the Thursday window and started deploying
          when things were ready.
        </p>

        <h2>What I work on now</h2>
        <p>
          Mostly the overlap between platform, security and reliability — the OSS
          licence audit, the policy-as-code deployment permissions, and the
          database access governance work all came out of the same stretch.
        </p>
        <p>
          I also self-host a fair amount at home. Running an identity provider, a
          reverse proxy and a search cluster yourself teaches you things quickly,
          and several things I've since deployed at work I broke at home first.
        </p>
        <p>
          I still write Swift, and I'm currently building the product's Flutter
          client, so the mobile side hasn't gone away. It just isn't the main
          thing any more.
        </p>

        <hr class="hair my-10" />

        <p>
          If you're hiring for platform, DevSecOps, SRE or cloud architecture
          work — or want to compare homelab notes —
          <a href={site.socials.email}>{site.email}</a>.
        </p>
      </div>

      <aside class="space-y-4 lg:sticky lg:top-24 lg:self-start">
        <div class="card p-5">
          <h2 class="font-mono text-xs uppercase tracking-[0.12em]" style="color: var(--c-text-faint);">Arc</h2>
          <ol class="mt-4 space-y-3">
            {arc.map((event) => (
              <li class="flex gap-3 text-xs leading-relaxed">
                <span class="shrink-0 font-mono font-semibold" style="color: var(--c-accent);">{event.year}</span>
                <span style="color: var(--c-text-dim);">{event.label}</span>
              </li>
            ))}
          </ol>
        </div>

        <div class="card p-5">
          <h2 class="font-mono text-xs uppercase tracking-[0.12em]" style="color: var(--c-text-faint);">Education</h2>
          <p class="mt-3 text-sm font-medium" style="color: var(--c-text);">{education.degree}</p>
          <p class="mt-1 text-xs" style="color: var(--c-text-dim);">{education.specialisation}</p>
          <p class="mt-3 text-xs" style="color: var(--c-text-dim);">{education.institution}</p>
          <p class="mt-1 font-mono text-xs" style="color: var(--c-text-faint);">
            {education.period} · {education.grade}
          </p>
          <ul class="mt-4 flex flex-wrap gap-1.5">
            {education.coursework.map((course) => (
              <li class="rounded border px-1.5 py-0.5 text-[0.68rem]"
                  style="border-color: var(--c-border); color: var(--c-text-faint);">
                {course}
              </li>
            ))}
          </ul>
        </div>

        <div class="card p-5">
          <h2 class="font-mono text-xs uppercase tracking-[0.12em]" style="color: var(--c-text-faint);">Elsewhere</h2>
          <ul class="mt-3 space-y-2 text-sm">
            <li><a href={site.socials.github} target="_blank" rel="noopener" style="color: var(--c-info);">GitHub ↗</a></li>
            <li><a href={site.socials.linkedin} target="_blank" rel="noopener" style="color: var(--c-info);">LinkedIn ↗</a></li>
            <li><a href={site.socials.credly} target="_blank" rel="noopener" style="color: var(--c-info);">Credly ↗</a></li>
            <li><a href={site.resumePath} style="color: var(--c-info);">Résumé (PDF)</a></li>
          </ul>
        </div>
      </aside>
    </div>
  </section>
</BaseLayout>
